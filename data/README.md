# Data workspace

Only small, licence-approved fixtures and generated synthetic data belong in Git. Raw downloads and private files are excluded. Dataset provenance records will be added before any external dataset is imported.

